// 숫자
let n: number = 10;
// n = 'hi'; // -> n 은 type이 number 이기 때문에 문자열 할당 불가

// 문자
let str: string = 'Hello';

// 논리
let bool: boolean = true;

// Undefined
let und: undefined = undefined;

// Null
let nul: null = null;

// Symbol
let sym: symbol = Symbol();
